{# <div {{ attributes }}>

 <h1>المجموعات</h1>
  <a href="{{ path('main.index') }}">الرئيسية</a>
  <h1>55555555555</h1>
</div> #}

<div {{ attributes }}>
  <main class="container-md">
    <div class="card">
      <div class="card-header bg-info">
        <div class="row">
          <div class="col-4">
            <h3>المجموعات</h3>
          </div>
          <div class="col-6">
            <input class="form-control" type="search" data-model="query" />
          </div>
          <div class="col-2">
            <a href="{{ path('groupe.create') }}"><i class="bi bi-plus-square-fill h1 text-success float-start"></i></a>
          </div>
        </div>
      </div>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table table-bordered">
            <thead>
              <tr>
                <th>التعيين</th>
                <th>المدين</th>
                <th>الدائن</th>
                <th>الرصيد</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              {% for gro in this.groupes %}
                <tr>
                  <td>{{ gro.intitule }}</td>
                  <td>{{ gro.debit|number_format(2, ',', '.') }}</td>
                  <td>{{ gro.credit|number_format(2, ',', '.') }}</td>
                  <td>{{ (gro.debit - gro.credit)|number_format(2, ',', '.') }}</td>
                  <td>
                    <a href="{{ path('groupe.edit', { id: gro.id }) }}"><i class="bi bi-pencil-square h2 text-success"></i></a>
                  </td>
                </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </main>
</div>

